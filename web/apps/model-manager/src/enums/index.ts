import { SORT_ENUM } from './antd_enum_convert';
import MODEL_ICON_LIST from './model_icon';
import PERMISSION_CODES from './permission_codes';
import REGEXP from './regexp';

const ENUMS = {
  SORT_ENUM,
  MODEL_ICON_LIST,
  PERMISSION_CODES,
  REGEXP,
};

export default ENUMS;
