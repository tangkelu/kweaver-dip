export enum LoadStatus {
  Loading = 'loading',
  Empty = 'empty',
  Failed = 'failed',
  Normal = 'normal',
}

// 指标的常量枚举
export enum MetricConstants {
  // 分组内指标模型数量
  MetricModelCount = 'metric_model_count',
}
